const api=require('../../utils/api');Page({data:{items:[],loading:false,error:''},onShow(){this.load()},async load(){this.setData({loading:true,error:''});try{this.setData({items:await api.paperMaterials()})}catch(e){this.setData({error:e.message})}finally{this.setData({loading:false})}},detail(e){wx.setStorageSync('paper-group',this.data.items[e.currentTarget.dataset.index]);wx.navigateTo({url:'/pages/paper/detail'})}})
